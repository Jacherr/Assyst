export interface ImageScriptTag {
    name: string
    content: string
    owner: string
    uses: number
}

export interface ImageScriptPackage {
    name: string
    content: string
    owner: string
}
